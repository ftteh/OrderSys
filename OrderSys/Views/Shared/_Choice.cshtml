<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <link href="~/Content/choice.css" rel="stylesheet" type="text/css" />
</head>
<body>
    <header class="header-banner" id="i73c">
        <div class="container-width">
            <div class="logo-container">
                <div class="logo">Home</div>
            </div>
            <nav class="menu">
                @if (User.Identity.IsAuthenticated)
                {
                    if (User.IsInRole("admin"))
                    {
                        <div class="menu-item">@Html.ActionLink("Create", "Create", new { controller = "Choices" })</div>
                        <div class="menu-item">@Html.ActionLink("Update Menu", "Update")</div>
                    }
                    else if (User.IsInRole("customer"))
                    {
                        <div class="menu-item">Misc<br /></div>
                        <div class="menu-item">Sales</div>
                        <div class="menu-item">Orders</div>
                    }
                    <div class="menu-item">@Html.ActionLink("Logout", "Logout", new { controller = "AuthAuth" })</div>
                }
            </nav>
            <div class="clearfix"></div>
            <div class="lead-title">COOK4U</div>
            <div class="sub-lead-title">All dishes are prepared with the freshest ingredients and ❤.</div>
        </div>
    </header>
    @RenderBody()


    <script src="~/Scripts/jquery-1.10.2.min.js"></script>
    <script src="~/Scripts/bootstrap.min.js"></script>
</body>
</html>